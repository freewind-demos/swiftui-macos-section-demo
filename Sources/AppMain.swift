import SwiftUI

@main
struct SectionApp: App {
    var body: some Scene {
        Window("Section", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 400)
    }
}